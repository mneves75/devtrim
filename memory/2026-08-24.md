# 2026-08-24

- Prepared 0.3.2 with an AS-IS/data-loss notice in the binary and public docs, confirmation for every interactive mutation, exact `-y` versus `--yolo` semantics, additional protected aliases, saturating totals, and fail-closed iCloud measurement.
- Reviewed all Rust source under the Rust skill, ran the full security toolchain and MSRV 1.85 locally, and closed autoreview's P2 documentation mismatch plus the independent verifier's P3 acknowledgment ambiguity; both post-fix reviews were clean.
- Published immutable, attested `v0.3.2-beta1`, then promoted the exact same ZIP and checksum into verified immutable `v0.3.2` production without rebuilding.
- Built the original Ratatui 0.4.0 TUI as a presentation adapter over the existing scanners and apply owners; immutable approval carries the exact preview, Trash purge consumes exact children, and CLI bypass flags cannot pre-authorize the interface.
- The dependency review rejected Ratatui 0.29 because its mandatory `lru 0.12.5` hit two RustSec soundness advisories; Ratatui 0.30.2 with default features off resolves patched `lru 0.18.2` and raises the proven MSRV only to 1.88.
- Two P3 autoreview passes found six real TUI defects: raw terminal warnings, JSON escaping at the wrong layer, clipped confirmation, hidden undersized input, lost alternate-screen diagnostics, and unscrollable partial errors. All were fixed and covered by deterministic tests.
- Final proof covered 52 unit tests, 17 CLI tests, exact Rust 1.88, strict Clippy, RustSec, arm64 release build, structural deletion lint, release scripts, Gitleaks, TruffleHog, a cancel-only PTY sentinel, and responsive browser checks for the manual and landing page.
