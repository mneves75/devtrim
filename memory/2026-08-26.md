# 2026-08-26

- Ran a full comparison audit against Mole 1.52.0 (source at tag V1.52.0,
  installed keg, GitHub/API evidence). Mole's 2026 data-loss incidents
  (#823, #993, #1122, #1427, #1390) all trace to per-script safety rails;
  devtrim's typed single sink is the structural answer. Verified devtrim gaps:
  multi-ecosystem purge, Homebrew distribution, apply journal, liveness
  guards, fuzzing, protect config, CACHEDIR.TAG, completions/man page.
- Plan 009 opened: close all verified gaps in 0.5.0 on the existing rails.
- Implemented via Codex lane (3 review rounds, all gates green each round):
  `clean artifacts` (closed corroborated-name list + CACHEDIR.TAG signature,
  stale-repo gate, apply re-verification), `protect` config enforced inside
  `validate_path_for_deletion`, write-ahead journal + `history` (+ `--json`
  single document), liveness guards (`pgrep`/`lsof` cwd probes, fail-closed),
  `completions`/`manpage`, BrokenPipe-tolerant stdout.
- Review corrections that mattered: Codex initially reordered roff output to
  satisfy a loosely-worded test (reverted; test relaxed instead), and probe
  failures initially returned empty findings with exit 0 — changed to Err so
  JSON automation sees the failure in the errors channel.
- Lane gotcha: `codex-lane next` must run with the same CODEX_HOME the lane
  started under (codex-auto had picked the berlin home); without it, resume
  fails with "no rollout found". Round 3 (fuzzing) tripped OpenAI's content
  filter mid-turn; Fable finished lib/bin split verification + fuzz runs.
- Independent security review + two autoreview P3 rounds found 13 real issues
  pre-release, all fixed with regression tests: protect Unicode NFC/NFD
  mismatch (macOS realpath returns the QUERY form, not stored form — verified
  empirically, so comparison must normalize), protect ancestor-deletion and
  symlinked-entry bypasses, GIT_DIR env poisoning of the stale gate,
  case-variant ambiguous names admitted via CACHEDIR.TAG, journal-failure
  truthfulness, partial-history exit codes, BrokenPipe panics, xcode probe
  silence, context-free command dispatch. My own refactor bug (taking
  cli.command before Ctx::from_cli silently rerouted ALL diagnostics to the
  TUI capture buffer) was caught by the hermetic CLI tests.
- Fuzzing: 5 targets, 0.5–1.4M execs/60s each, zero crashes; cargo-fuzz needs
  the nightly toolchain bin dir FIRST on PATH (homebrew standalone cargo
  breaks `+nightly` and cargo-fuzz's inner build).
