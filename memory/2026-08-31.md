# 2026-08-31

## Pre-production closeout

The final P3 autoreview reproduced one remaining data-loss path: a lowercase
`node_modules` target below an ancestor spelled `NODE_MODULES` could be scanned,
authorized, and permanently deleted on the default case-insensitive macOS
filesystem. Scanner pruning and both apply-time ancestor checks now share an
ASCII-case-insensitive namespace predicate, while only the exact lowercase leaf
remains actionable. Regression tests prove the prior deletion, the fixed refusal,
and an ordinary `node-modules` positive control.

The independent gate then found the same namespace gap in `artifacts` and Opus
5 found a direct `.git` Trash item blocking later purge targets. Both were
reproduced before repair. Artifact scan/apply now share the dependency boundary;
Trash preview leaves Git-metadata-named direct items with a warning while other
exact items proceed. The same closeout removed duplicate directory enumeration,
isolated Git fixtures from signing/hooks, proved Gitleaks control ordering, and
added syntax coverage for its helper.
