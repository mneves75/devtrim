# 2026-08-25

- Reconciled the v0.4.0 release candidate without rewriting public history: historical immutable releases and tags remain provenance records.
- Added a closed `CommandAuthority` capability so forged serialized Docker or simulator command actions cannot authorize execution; apply also checks the exact fixed argv variant.
- Split hosted release preparation from publication authority, added npm dependency maintenance and release gates for the demo video, and tightened shipped inline-script CSP to exact hashes.
- Fixed release retry idempotence: full reruns replace the intermediate handoff artifact, while publisher-only reruns refresh immutable release state before deciding whether to create or only verify.
- Decoupled artifact version checks from the production landing page so beta packages can describe v0.4.0 while the live site continues to advertise v0.3.2 until stable promotion.
- Built and installed the local arm64 v0.4.0 candidate at `~/.local/bin/devtrim`; preserved the prior v0.1.0 binary as a backup. Publication gates remain pending on the final combined diff.
- Final proof passed strict Clippy, 61 unit + 18 CLI tests on Rust 1.98 and MSRV 1.88, RustSec across 188 dependencies, npm audit/lint/format/build across 340 packages, actionlint/ShellCheck, structural positive controls, history and exact-worktree secret scans, arm64 build, disposable-HOME PTY cancellation, and responsive landing/manual browser flows.
- The exact package rehearsal produced the expected five ZIP entries; checksum, full license, README, manual, Mach-O arm64 architecture, v0.4.0 output, and equality with the locally installed binary all verified.
