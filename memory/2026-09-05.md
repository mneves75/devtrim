# 2026-09-05

## Production deployment

Completed the user's 0.8.1 production request through the existing release
scripts: staged the audited commit, verified the downloaded beta in isolated
terminals, promoted the exact immutable archive, and completed Homebrew
publication, audit, upgrade, and tests. Both hosted release runs passed.
An independent verifier matched tag commits and archive hashes. Release IDs,
checksum, and distribution state are in `MEMORY.md`.

The initial sandboxed preflight could not write `.git/FETCH_HEAD`; authorized
elevated execution completed both release scripts. No tag retries or source
changes were needed. Post-release documentation advances the stable download
links and opens 0.8.2 Unreleased.
