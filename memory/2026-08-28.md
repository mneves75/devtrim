# 2026-08-28

## CODING_STANDARDS.md rewritten, five invariants moved into gates

`CODING_STANDARDS.md` was two untracked lines feeding the `/code-review`
skill's Standards axis. Read the skill's source: standards discovery is one
fuzzy sentence that never names `AGENTS.md`/`CLAUDE.md`, citation is mandatory
(`file + the rule`), and the sub-agent is told to skip anything tooling
enforces. That dictated the shape — addressable `S<n>` rules, an explicit
"already enforced, do not report" table, and an "inherited invariants, cite do
not restate" table pointing at `SECURITY.md § Defense layers` and
`CLAUDE.md § Conventions` instead of duplicating them a fourth time.

Audit of `src/` found several invariants held at 100% with no gate behind them.
Those became gates, not prose:

- `Cargo.toml`: `unsafe_code = "forbid"` plus clippy `unwrap_used`,
  `expect_used`, `panic`, `unreachable`, `todo`, `unimplemented`, `dbg_macro`,
  `allow_attributes_without_reason`. All passed on the first run — production
  was already at zero. Cost: a `cfg_attr(test, allow(..., reason))` in
  `src/lib.rs`, a file-level allow in `tests/cli.rs`, and one reasoned allow on
  the `unreachable!` arm in `src/app.rs`.
- `rules/no-shell-invocation.yml` — the `CLAUDE.md` "never add shell-string
  execution" convention had zero automated protection until now.
- `rules/no-abbreviated-bindings.yml` — `|e|`/`Err(e)`/`let val` against a
  denylist, in binding positions only.

### Lessons

- An `#[allow]` on a macro *invocation* is silently ignored (`unused_attribute`
  fires). It has to go on the enclosing match arm.
- ast-grep `inside:` defaults to the immediate parent — `stopBy: end` is
  required or `|mut err|` and `Err(ref e)` slip through. And `stopBy: end`
  without `field: pattern` walks into parameter *types*, so
  `std::num::NonZeroU32` matched the `num` denylist entry. Both found by
  planting the violation in a scratch tree, not by reading the rule.
- Line numbers in a doc no gate checks are change detectors. Every precedent in
  `CODING_STANDARDS.md` cites file + a searchable phrase instead.
- autoreview P3 raised five findings across two passes; two were real (both my
  own new rules had false negatives), one was wrong (claimed CI never scans —
  `ast-grep scan` is the next line in the same step, outside the diff hunk), and
  one proposed a fix that would have misfired on `src/cli.rs`, where `"bash"`,
  `"zsh"`, `"fish"` are legitimately shell *names* for `completions`. Checking
  before applying was the whole value of the pass.
- The shell lint's residual gap (a shell reached through a variable) is closed
  by prose, not by a fragile structural allowlist — that is standard S12.

## Verified end to end after landing

Planted one tautological test on a throwaway branch — `assert!(sandbox.path().exists())`
and `assert!(sandbox.bin().is_dir())`, both guaranteed by a constructor that
`unwrap()`s on `create_dir_all` — and ran the Standards axis against `main`.

It reported a **hard violation of `CODING_STANDARDS.md` § S1**, cited the rule by
ID, quoted the hunk, traced why each assertion cannot fail (`bin()` creates the
directory it then asserts on), and prescribed S1's own fix. It labelled a Mysterious
Name as a judgement call and reported no spec rather than inventing one. Its closing
line is the proof the split works: "Skipped anything cargo fmt/clippy/ast-grep scan
enforces; this file trips none of those gates, which is precisely why S1 exists as a
written standard rather than a rule."

So the citation contract holds: an addressable `S<n>` beats a bullet, and the
"already enforced — do not report" table actually keeps the reviewer off ground
the gates own. The branch and its commit were deleted; `main` never carried the
planted test.

## 0.6.2 release candidate reviewed and verified

The full source/security pass found four real authority gaps: Docker could
follow a remote context, simulator apply delegated target selection back to a
broad owner command, `lsof` status 1 erased liveness uncertainty, and default
Trash could carry a nested worktree that permanent deletion refused. The fixes
bind the local Unix socket and individual UDID in `CommandAuthority`, make probe
and metadata uncertainty blocking, and preflight every directory before either
mutation mode. Focused positive controls prove each refusal happens before the
forbidden command or filesystem mutation.

The full current/MSRV suites, strict Clippy, structural rules and planted
controls, both dependency audits, five 60-second fuzz targets, arm64 build, PTY
cancel flow, video build/render, shell/workflow policy, secret scans, and
desktop/mobile browser checks passed. P3 autoreview returned no actionable
finding; a fresh verifier independently returned PASS. The v0.6.2 local
candidate is ready for beta staging; v0.6.1 remains production until promotion.

## 0.6.3 full review

The new audit compared the deletion-authority design with current Rust
filesystem safety guidance and Mole's purge behavior, then probed every
candidate finding with positive controls before changing code. Six findings
survived that check: complete command actions were not escaped at the final
human sink; the TUI Trash preview retained configured protected items; a
present but invalid `swift-latest.xctoolchain` could look like an empty
success; irrelevant global mutation flags were silently accepted; release
version gates used substring evidence; and Xcode/toolchain apply trusted the
shared path sink without reasserting the category scanner's exact shape.

Each gap now has a regression that first proves the vulnerable or forbidden
case is observable, plus a valid-path control where relevant. The release
candidate is 0.6.3; production `index.html` intentionally stays on stable
0.6.2 until the staged artifact is explicitly approved for promotion.

P3 autoreview then found one integration edge the initial matrix missed:
`devtrim --json` with no explicit subcommand could enter the implicit TUI on a
real terminal. A PTY positive control reproduced the alternate-screen launch;
the shared validator now rejects JSON for both implicit and explicit TUI, and
the same PTY exits immediately with status 2 and one JSON error document.

The follow-up P3 pass found one final capability mismatch: Docker and simulator
cleanup accepted filesystem-only `--shred` even though both preview and apply
use exact typed command actions. The validator now rejects that flag before any
probe, with JSON regressions for both targets and the valid filesystem preview
control retained.

The controlled Mole 1.52.0 comparison used 20 stale and 5 recent Git projects,
each with one 65,536-byte `node_modules`. Both tools found exactly the 20 stale
trees (1,310,720 bytes) and excluded every recent control. Across 15 alternating
samples while one-minute load average rose from 16.46 to 18.48, devtrim averaged
0.481 s (0.410–0.590) and Mole 5.800 s (5.220–7.440); peak RSS was 8,667,136
versus 9,502,720 bytes. The result applies only to that shared scanner path,
because Mole covers a broader cleanup taxonomy.

Final current/MSRV tests, strict Clippy, fmt, structural controls, root/fuzz
RustSec audits, release policy, demo checksum/container, P3 autoreview, both
Matt Pocock review axes, and a fresh independent verifier passed. The v0.6.3
tree is ready to commit and stage as beta; production remains gated on explicit
post-staging confirmation.
