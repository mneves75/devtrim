# 2026-08-23

- Audited the 0.2.1 deletion boundary before preparing 0.3.0.
- Independent review found an owner-cache escape and a lossy display-path
  round-trip in addition to the already repaired protected-path bypass.
- Reworked findings, apply outcomes, command allowlist tests, property tests,
  release gates, and documentation around a private verified target.
- Full local gates, two autoreview passes, independent verification, commit, push, and exact-commit CI completed successfully. Release publication remains explicitly out of scope.
- Published `v0.3.0-beta1`; remote verification showed that it was mutable and lacked attestations, so it is retained as historical evidence but cannot be promoted.
- Reworked release ownership: hosted CI builds and attests an immutable beta once; production may only promote the same verified bytes from the same commit.
