# 2026-08-23

- Audited the 0.2.1 deletion boundary before preparing 0.3.0.
- Independent review found an owner-cache escape and a lossy display-path
  round-trip in addition to the already repaired protected-path bypass.
- Reworked findings, apply outcomes, command allowlist tests, property tests,
  release gates, and documentation around a private verified target.
- Full local gates, two autoreview passes, independent verification, commit, push, and exact-commit CI completed successfully. Release publication remains explicitly out of scope.
- Published `v0.3.0-beta1`; remote verification showed that it was mutable and lacked attestations, so it is retained as historical evidence but cannot be promoted.
- Reworked release ownership: hosted CI builds and attests an immutable beta once; production may only promote the same verified bytes from the same commit.
- Published and verified immutable, attested `v0.3.0-beta2`, proving exact-byte promotion prerequisites before production.
- A fresh security review found three additional authority gaps: partial directory measurements, ignored config typos, and permissive Docker size parsing. Hardened each shared boundary and made Gitleaks/TruffleHog executable release gates for 0.3.1.
- Published immutable, attested `v0.3.1-beta1` after exact-commit CI and independent artifact verification.
- The first production workflow failed before publication because shell interpolation produced an invalid jq regex escape. Kept the final tag fixed, corrected the selector on `master`, and manually promoted the byte-identical beta ZIP/checksum into verified immutable `v0.3.1` production.
- Fixed the manual's desktop grid after its skip link auto-placement displaced the table of contents and content; explicit grid areas now preserve desktop, mobile, and print layout, with zero automated WCAG violations.
