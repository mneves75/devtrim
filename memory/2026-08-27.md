# 2026-08-27

- 0.6.0 built and hardened. Research (Rust CVE-2022-21658 remediation,
  cap-std, gnulib fts, Apple signing docs, lumberjack rotation) drove the
  design; plan 010.
- Shipped: identity-verified parent-anchored deletion sink (cap-std) with a
  quarantine protocol for permanent deletes — rename to a private
  RandomState-token name, re-verify, open the quarantined dir and drive
  recursive deletion through the handle (`remove_open_dir_all`); `devtrim
  largest` (report-only, overlapping roots normalized); journal rotation
  (shift-rename at open under a real rustix flock, size re-checked under the
  lock, history reads generations under a shared lock); explicit ad-hoc
  codesign in the release workflow; demo video re-rendered to the current
  menu and frame-verified.
- Notarization: a Developer ID Application cert EXISTS locally; notarytool
  credentials do not. Runbook documented in SECURITY.md; workflow machinery
  deliberately not implemented while untestable. macOS realpath insight from
  yesterday held; new insight: Homebrew formulae/curl never set quarantine —
  Gatekeeper only bites browser downloads.
- Review economics again positive: security agent (SHIP + 2 minor) and two
  autoreview rounds (5 + 2 findings) — all real, all fixed: sink leaf-race
  closed via handle-bound recursion, special-file deletion restored, rotation
  double-rotation and stale-lock races replaced by flock, history snapshot
  under lock, largest exit-code contract and root overlap double-count.
- Autoreview refuses binary diffs: commit rendered media separately (frame-
  verified) before running the review on the text diff.
- Codex round 3 content-filter incident from yesterday did not recur; lane
  devtrim-060 ran 2 rounds cleanly under CODEX_HOME=berlin.
- 0.6.1 production-hardening candidate implemented after adversarial audit:
  one-document JSON for Clap failures, truthful partial/empty outcomes,
  fail-closed scanner/probe/traversal semantics, recursive iCloud logical/local
  allocation inventory, nested Git/worktree and foreign-device refusal, macOS
  generation rechecks, and protect-alias revalidation.
- Journal boundary rebuilt around no-follow directory handles: parents and
  leaves refuse symlinks, writers serialize synced records across processes,
  rotation cannot split attempt/result, history is bounded/linear and creates
  no lock or directory. Unit stress used a real child process; real-binary
  probes preserved a cache behind a symlinked state path and proved history
  read-only.
- Release authority was separated: the local credential-bearing script now
  checks provenance only; read-only hosted jobs run deterministic/fuzz/video/
  secret gates and create the handoff; a no-checkout publisher alone receives
  release-write/OIDC. Production still copies the highest immutable attested
  beta from the same commit and verifies exact assets/checksums/signer.
- Local candidate evidence so far: Rust format/Clippy/full tests (121 library
  pass + 1 helper ignored, 48 CLI pass) on current and MSRV 1.88; root/fuzz
  RustSec audits; ast-grep positive controls; release policy, shellcheck,
  actionlint, Gitleaks and TruffleHog; npm audit/lint/format/build; real PTY
  cancel; arm64 v0.6.1 build; desktop/mobile browser checks; five fuzz targets
  at 60 seconds each. TruffleHog exited 0 with zero findings but logged a
  sandbox-only failure cleaning its temporary process artifacts.
- Independent blocker re-review cleared nested-worktree deletion, journal
  no-follow paths, concurrent append/pairing, and read-only history. Two P3
  autoreview rounds found four verified P2 defects: quarantine restoration
  before removal, symlinked scan roots, a vacuous path-validation fuzz oracle,
  and history caps incompatible with valid rotated writer output. All four were
  fixed with focused regressions; the final history reader reverse-scans fixed
  chunks, stops at the requested newest tail, and bounds inspected bytes from
  the requested limit and maximum record size.
- Final independent security review approved the gate with no P0/P1, then
  identified two audit-truth defects: legacy attempt/result records split
  across rotations were double-reported, and a concurrent active apply could
  appear interrupted. Pairing state now spans generations and history takes an
  exclusive read-only snapshot lock, waiting for guarded attempts to finish;
  focused concurrency and legacy regressions pass. A narrow independent
  re-review cleared both remediations and confirmed bounded scanning plus
  read-only/no-create behavior remained intact.
- The first 0.6.1 staging attempt (`v0.6.1-beta1`) failed before any build or
  publication: GitHub Actions `GITHUB_TOKEN` cannot call the immutable-release
  settings endpoint, which requires repository Administration read and has no
  workflow permission equivalent. The admin-level check remains in the local
  provenance-only script; hosted jobs retain the release object's immutable
  postcondition and now reject reintroducing the impossible API call. A narrow
  independent security re-review cleared this credential-minimizing design.
